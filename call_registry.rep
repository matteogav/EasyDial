struct node_hash {
	phone _phone;
	node_hash* _seg;
	node_hash(const phone phone, node_hash* seg = NULL);
};

node_hash **_taula;
nat _mida;
nat _n_elements;

nat hash(nat num) const;		//funcio de hash
void rehash();
void afegir(phone p);
float factor_carrega(nat mida_taula, nat n_elements);
