struct node_hash {
	phone _phone;
	node_hash* _seg;
	node_hash(const phone &p, node_hash* seg = NULL);
};

node_hash** _taula;
nat _mida;
nat _n_elements;

nat hash(nat num) const;		//funcio de hash
nat hash_c(string nom) const;
void rehash();
node_hash* consulta(nat num, nat &k) const;
